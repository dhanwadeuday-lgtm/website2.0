{
  "name": "snickylink",
  "version": "3.0.0",
  "description": "SnickyLink landing page (Velvet & Ember, registration-gated snick lock) + waitlist API (Vercel serverless + Upstash Redis)",
  "private": true,
  "scripts": {
    "dev": "vercel dev"
  },
  "dependencies": {
    "@upstash/redis": "^1.31.6"
  },
  "devDependencies": {
    "vercel": "^37.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}
