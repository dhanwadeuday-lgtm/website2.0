{
  "name": "snickylink",
  "version": "1.0.0",
  "description": "SnickyLink launch site (Website 2.0 — vine/bloom cinematic redesign) + real cross-device pairing API (Vercel serverless + Upstash Redis)",
  "private": true,
  "scripts": {
    "dev": "vercel dev"
  },
  "dependencies": {
    "@upstash/redis": "^1.31.6"
  },
  "devDependencies": {
    "vercel": "^37.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}
